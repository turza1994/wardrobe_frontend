'use client';
// TODO: Implement OTPInputForm (6-digit, auto-focus, paste support)
export function OTPInputForm({ onComplete }: { onComplete: (otp: string) => void }) {
  return <div role="group" aria-label="OTP input">OTPInputForm</div>;
}
